# Clean URLs for the pre-planning funnel. A rewrite (200), not a redirect:
# the address bar must keep /preplan/coordonnees so a refresh, a bookmark or a
# link forwarded to a spouse all reopen the same step.
/preplan            /plan.html   200
/preplan/*          /plan.html   200
